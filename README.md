# LM Studio with python

### install dependencies

```bash
$ uv sync
```

### run

* run embedding

```bash
$ uv run embedding.py
```

* run chat completion

```bash
$ uv run chat-completion.py
```

* run stream

```bash
$ uv run chat-stream.py
```